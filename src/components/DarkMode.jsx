import styled from 'styled-components';

const DarkMode = () => {
  return (
    <Container>
      DarkMode
    </Container>
  );
};

const Container = styled.div`
  width: 4.5rem;
  height: 4.5rem;
`;

export default DarkMode;
